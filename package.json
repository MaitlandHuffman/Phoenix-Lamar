{
  "name": "phoenix-lamar",
  "version": "1.0.0",
  "description": "Phoenix Lamar Quoting System",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm start --prefix frontend\" \"npm run dev --prefix backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
